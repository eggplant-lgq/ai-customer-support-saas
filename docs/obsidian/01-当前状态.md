# 当前状态

## 当前阶段

阶段 0：项目地基。

## 已完成

- 初始化 Next.js App Router 项目。
- 整理为 monorepo 结构，Next 应用位于 `apps/web`。
- 添加 Docker Compose，包含 PostgreSQL + pgvector 和 Redis。
- 添加 `.env.example`，用于跨设备复制本地配置。
- 添加 `AGENTS.md`、`README.md` 和 Obsidian 笔记结构。
- 选择 Prisma 作为第一阶段数据库工具，更适合先学习主流 SaaS 数据建模、CRUD 和 migration。
- 已生成 Prisma Client。
- 已将 Next 默认远程字体改为系统字体，避免离线或 CI 构建时访问 Google Fonts 失败。
- 已通过 `pnpm lint`、`pnpm build`、`pnpm db:generate`。
- 已启动 Next dev server，并确认 `http://localhost:3000` 返回 200。
- Docker 已安装可用。
- Redis 镜像已下载成功。
- 第一阶段 Docker 数据库镜像已调整为 `postgres:16-alpine`，先学习标准 PostgreSQL；pgvector 等到 RAG 阶段再引入。
- `docker compose up -d postgres` 已尝试启动，但 Docker Hub 在获取 PostgreSQL 镜像下载 token 时多次 `EOF`，属于 Docker Hub 网络问题，不是项目配置问题。
- GitHub remote 已关联并推送成功：`git@github.com:eggplant-lgq/ai-customer-support-saas.git`。

## 本机环境提醒

- Node.js 已可用。
- pnpm 已可用。
- Docker Desktop 已安装可用，但 Docker Hub 连接不稳定，可能需要配置镜像源或稍后重试。

## 下一步建议

1. 配置 Docker 镜像源或稍后重试 `docker compose up -d postgres`。
2. 数据库启动成功后，生成并应用第一版 Prisma migration。
3. 提交 migration，并解释 Prisma schema、migration、数据库表关系。
4. 开始阶段 1：后台基础布局、登录和团队空间。

## 新会话恢复提示词

```text
请先阅读 AGENTS.md、README.md、docs/obsidian/01-当前状态.md，继续带我做下一步。先用中文解释今天要学什么，再给验收标准，然后再开始实现。
```
